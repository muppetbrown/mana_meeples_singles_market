# apps/web/.env.local
# Local development - point to local API server

# Point to your local API server (no /api suffix - endpoints will add it)
VITE_API_URL=http://localhost:10000

# Optional: If you want to test against staging API instead
# VITE_API_URL=https://api-staging.manaandmeeples.co.nz

# Optional: Fallback (probably not needed for local dev)
# VITE_FALLBACK_API_URL=http://localhost:10000